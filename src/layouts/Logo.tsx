import { Link } from "react-router-dom";
import './Logo.scss';

const Logo = () => {
    return (
        <Link to="/">
            <p data-text="TBD" className="logo">TBD</p>
        </Link>
    );
};

export default Logo;
